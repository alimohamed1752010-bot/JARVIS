# JARVIS V14.0.0 — SUPERIOR

V14 adds an autonomous reasoning layer: JARVIS correlates events, investigates suspicious activity with audit logs and server knowledge, and recommends actions without silently taking destructive action.

Natural language examples:
- "Jarvis, investigate what's happening."
- "Jarvis, why are the roles changing so fast?"
- "Jarvis, is this join spike suspicious?"
- In a DM, reply directly to JARVIS without saying his name.

Railway setup remains compatible with the V13.5 environment. Use the existing `DISCORD_TOKEN`, `GEMINI_API_KEY`, and owner configuration.
