# JARVIS V13.0.0 — SUPERIOR RELEASE NOTES

## New
- Persistent server knowledge layer (`src/core/serverKnowledge.js`).
- Per-server fact/event memory with bounded retention.
- Startup server scan and cached structural knowledge.
- Server knowledge injected into conversational AI and agent planning context.
- Bounded autonomous repair loop after partial plan failure.
- Repair plans use current live state and are capped by `JARVIS_AGENT_LOOPS` (default 2, max 4).
- Server knowledge tools: `server.knowledge` and `server.scan`.
- Additional tests for persistent knowledge, bounded repair loops, and DM continuity.

## Preserved
- Natural-language multi-step Discord administration.
- Risk engine, plan validation, confirmation flow, snapshots, action journal and verification.
- Six-model fallback AI configuration.
- Tony Master Mode / non-master Roast Mode.
- DM reply-to-JARVIS invocation without requiring the name.

## Validation
- JavaScript syntax checks passed.
- Automated suite: 10/10 tests passed.
