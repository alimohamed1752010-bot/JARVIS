# JARVIS V20.5

- Restored actual Windows WAV playback for local Kokoro TTS.
- Improved faster-whisper decoding with beam search and tighter VAD settings.
- Prevented third-party model-loading stdout warnings from corrupting the JSON worker protocol.
- Added a local PC fallback for simple open/close/volume/Spotify voice commands when the remote voice bridge returns an empty response.
- Web-site close commands close the active Brave tab only when the requested site is visibly active.
